/*const swagger=require('swagger-client');

let app=require('./server').app;

const request={
	url,
	query,
	method,
	body,
	headers
};

swagger.http(request)
	.then((res)=>{
		res.statusCode
		res.body
	});*/